# AHB 学习资料库：Markdown 学习文档编写规范

本规范用于统一仓库中后续 AHB、AMBA 及相关片上总线学习文档的写法。目标是让每篇文档都能作为独立的中文学习资料使用，同时保留与官方规范的可追溯关系。

## 1. 规则等级

本文使用以下三个等级：

- **必须：** 发布前必须满足；主要涉及事实准确性、资料可追溯性、术语一致性、链接有效性和基本排版质量。
- **推荐：** 默认采用；可以根据主题和篇幅调整，但应保证调整后仍便于初学者阅读。
- **可选：** 用于增强教学效果；是否采用由文档类型和内容复杂度决定。

除非条目明确标为“推荐”或“可选”，包含“必须”“不能”“禁止”的条目均视为发布要求。

## 2. 内容依据与信息标记

- **必须：** 以 Arm/AMBA 官方 PDF、官方文档页面或官方培训材料为第一依据。
- **必须：** 在正文开始前标注文档名称、版本、Issue 和精读页码范围；无法获得其中某项时，应明确写明，而不是猜测。
- **必须：** 中文解释不能改变官方信号方向、时序条件或规范要求。
- **必须：** 如果内容来自不同版本，应明确版本差异，不能把 AHB-Lite、AHB5 和旧版 AHB 的定义混写。
- **必须：** 直接引用官方原文时注明章节或页码。中文意译应作为解释性正文，不能放入引号冒充官方原句。
- **推荐：** 按以下方式区分不同性质的信息：
  - **规范原意：** 对官方要求的忠实转述，注明对应章节或页码。
  - **理解提示：** 为初学者补充的背景、类比或直观解释，不改变规范原意。
  - **工程推断：** 根据规范得出的实现或验证层面结论，明确标注为推断，并说明推断依据。
- **推荐：** 对超出当前章节的内容标注“理解提示”或“后续章节”，不要用后文细节替代当前章节的原文含义。

## 3. 面向读者与术语

- **推荐：** 默认面向第一次系统学习 AHB 的读者。
- **必须：** 官方术语第一次出现时保留英文并给出中文或旧称，例如 `Manager（原 Master）`、`Subordinate（原 Slave）`。
- **必须：** 一篇文档选定主要术语后保持一致，不能在 Manager/Master、Subordinate/Slave 等名称之间无规律切换。依据旧版规范而保留旧称时，应在开头说明版本背景。
- **推荐：** 先解释角色和数据流，再解释信号和编码，最后给出规范细节与自测题。
- **推荐：** 复杂概念优先用“第一步、第二步、第三步”的实际传输流程解释。

## 4. 文档类型与结构

不同文档不必强行使用同一套完整结构。编写前应先判断文档类型：

### 4.1 章节精读

适用于按官方规范章节系统整理的长篇学习资料，推荐结构如下：

1. 标题、原始 PDF 链接、版本、精读范围和前置阅读。
2. 面向读者说明、术语约定、颜色语义和信息标记。
3. 按规范原章节顺序讲解协议规则，并通过关键接口图或时序图解释数据流、握手和等待行为。
4. 自测题。
5. 独立的“问题记录与解答”章节，集中保存阅读过程中实际提出的问题、截图和完整答案。
6. 本篇边界、后续章节阅读路线和资料来源。

章节精读默认使用 `<details>` 折叠一级内容，`<summary><strong>编号. 标题</strong></summary>` 作为章节入口，并按需展开。“问题记录与解答”是例外：章节标题保持可见，每个问题分别使用独立的 `<details>` 折叠。

学习目标不再单独重复列出，改为与已有题目去重后统一放入自测题。使用 `<details>` 编写自测题时，参考答案必须使用 Markdown 引用块（每段以 `>` 开头）缩进，多段答案之间保留空行。

实际提问与自测题必须分开：自测题用于覆盖章节知识点，实际提问则按产生顺序集中放在“问题记录与解答”中。每个问题的入口必须紧跟实际引发问题的具体位置，例如对应图片的图注、表格、规则段落或地址示例之后；不能只放在同一章节开头、概括性表格或其他相近但并非实际提问来源的位置。每个问题的显式锚点必须放在对应 `<details>` 之前，使链接即使在答案折叠时也能跳转到可见的问题标题；不能把所有问题包在一个默认关闭的外层 `<details>` 中。

### 4.2 专题辨析或问题解答

适用于单个信号、时序问题或易错概念的短篇说明。推荐至少包含：

1. 问题与直接结论。
2. 适用的协议版本或上下文。
3. 结合信号方向或传输阶段进行解释。
4. 常见误解与正确理解。
5. 官方依据和相关专题链接。

短篇说明不强制包含完整术语表、自测题或后续章节路线。

### 4.3 仓库索引

适用于资料库入口或导航 README。推荐至少包含：

1. 仓库用途和内容边界。
2. 按学习顺序或主题分类的文档链接。
3. 原始规范与外部资料入口。
4. 文件命名、图片目录或贡献约定的入口。

索引文档以导航为主，不重复专题正文。

无论采用哪种结构，都不需要为了“看起来完整”重复图中已经清楚表达的端口表。图下方应解释箭头、角色和时序含义，而不是逐项抄录图片。

## 5. 章节精读标准模板

新建章节精读文档时，默认复制下面的模板。模板中的编号和主题名称可以按实际章节调整，但“正文—自测题—问题记录与解答—边界与资料来源”的顺序应保持一致：

```markdown
# AMBA AHB Protocol Specification：第 N 章主题精读

> 原始资料：[资料名称](相对链接)<br>
> 精读范围：Chapter N，PDF 页码和文档页码<br>
> 文档版本：Issue、文档编号、发布日期<br>
> 前置阅读：[上一章或相关专题](相对链接)

本文面向第一次系统学习 AHB 的读者，说明本章的核心规则和进阶内容。首次出现时使用 `Manager（原 Master）`、`Subordinate（原 Slave）` 等新旧术语对照，此后保持术语一致。

> **颜色约定：** 蓝色表示关键协议名和信号，橙色表示限制和易错条件，绿色表示正确方向和结论。
>
> **信息标记：** 正文默认转述规范原意；“理解提示”用于补充直观解释；“工程推断”用于说明实现或验证结论。

<details>
<summary><strong>1. 核心主题</strong></summary>

### 1.1 规范规则

先说明规范原意和适用条件，再按角色、信号方向或传输步骤解释。

![能够概括图意的替代文本](assets/topic/figure-n-n-topic.png)

*图 1：原规范 Figure N-N，说明图意、来源文档、页码和版权信息。*

> **相关问题：** [这里为什么会出现某种现象？](#question-example)

> **理解提示：** 这里放帮助初学者建立直观认识的补充说明。

> **工程推断：** 这里放根据规范得出的实现或验证结论，并说明依据。

</details>

<details>
<summary><strong>2. 自测题</strong></summary>

<details>
<summary>1. 能否解释本章最核心的规则？</summary>

> 参考答案使用 Markdown 引用块，并写清条件和例外。

</details>

</details>

## 3. 问题记录与解答

<a id="question-example"></a>

<details>
<summary><strong>3.1 这里为什么会出现某种现象？</strong></summary>

![完整保留提问上下文的问题截图](assets/topic/question-example.png)

*问题截图：说明箭头或标记指向的位置，保留提问时的完整上下文。*

先准确复述问题，再按不同传输、阶段或信号归属逐步解释，最后给出明确结论。

> <span style="color:#63d297"><strong>结论：</strong>用一句话回答问题，并说明判断时真正应该检查的条件。</span>

</details>

<details>
<summary><strong>4. 本篇边界与资料来源</strong></summary>

说明本文没有覆盖的内容，以及应继续阅读的章节或专题。

资料来源：

- [官方资料名称](链接)，版本、章节或页码。

</details>
```

模板中的资料版本、适用范围、正文依据、问题锚点和资料来源属于必须信息；颜色提示、理解提示、工程推断和问题截图按内容需要选用。若本章尚未产生实际问题，仍可保留“问题记录与解答”入口并注明“暂无”，后续新增问题时不改变其他章节编号。

## 6. 配图和流程图标准

- **推荐｜优先使用原图：** 如果官方 PDF 或 Arm 官方网页已有对应框图、接口图或时序图，直接渲染、裁剪并引用原图。
- **必须｜图像质量：** 使用高分辨率渲染；裁剪时去掉无关正文、页眉和页脚，但保留图号和必要留白。
- **必须｜图注格式：** 说明原图编号、图意、来源文档和页码；注明“图中未包含 AHB5 新增信号”等重要限制。
- **必须｜图文讲解顺序：** 每张用于讲解协议的图片统一按“图片 → 图解 → 结论”组织。图注只说明图片身份和范围，不能代替图解；不能跳过图解直接给出结论。
- **必须｜图解方式：** 时序图按时钟或传输阶段的先后顺序解读信号变化；结构图、接口图和数据通路图按角色、箭头或数据路径逐项解读。图解只说明从图中能够观察和推导出的内容，避免复述无关背景。
- **必须｜图后结论：** 图解结束后使用一句或少量短句概括该图直接支持的核心结论，不重复整段图解，也不扩展到与该图无关的问题。
- **禁止纯文本作图：** 不使用由 `│`、`─`、`▼` 等字符组成的框图或时序图。
- **推荐｜流程表达：** 有官方图时优先使用官方图；没有对应官方图时，可使用 Mermaid 生成可缩放的箭头流程图，但节点必须表达实际角色、信号和方向，不得只写笼统的“系统处理”。
- **推荐｜流程图布局：** 流程较长时使用纵向布局；读写、成功/失败等分支应明确标注。
- **推荐｜节点文字结构：** Mermaid 节点优先采用“短标题 + 一句说明 + 独立信号行”的结构；标题概括动作，说明补充角色或结果，信号行集中列出接口信号。
- **必须｜文字与换行：** 不得拆开中文词语、英文术语或信号名换行，必要时应加宽节点或主动分行；中文说明以左对齐为主，短标题或判断节点可居中。
- **推荐｜信号排版：** 信号名使用反引号标记并尽量单独成行；多个信号之间使用一致的分隔符，避免与长句混排造成视觉拥挤。
- **推荐｜层级与留白：** 标题使用较醒目的字重，正文使用常规字重；节点内保持足够的上下留白，避免通过缩小字号容纳过多文字。
- **推荐｜分支标签：** 读/写、成功/失败等分支使用简短且对称的标签，分支节点的标题和说明保持相同的表达层级。
- **必须｜替代文本：** Markdown 图片应填写能概括图意的替代文本，不能只写“图片”或留空。
- **必须｜图片目录：** 新文档的图片统一放在 `assets/<topic>/` 下，文件名使用英文和图号，例如 `figure-3-3-read-transfer-two-waits.png`。已有 `assets/images/` 内容在相关专题后续维护时逐步迁移，不为目录统一单独进行批量改名。
- **必须｜问题截图：** 为保留问题的完整性，带箭头、标记或提问上下文的原始截图应完整复制到 `assets/<topic>/`，不得裁掉问题所依赖的波形、图注或上下文，也不得进行有损压缩。复制后应核对尺寸；能够取得原文件时，同时核对 SHA-256。
- **推荐｜问题截图命名：** 使用 `question-<short-topic>.png`，并在图注中说明标记指向的位置以及截图完整保留的原因。

## 7. 信号解释标准

- **必须：** 明确区分输入、输出以及系统汇总后的信号。
- **必须：** 解释 `HREADY` 时说明它是全局传输完成指示，不把它简单写成某个 Subordinate 的“准备信号”。
- **必须：** 每个重点信号至少说明谁驱动、谁接收、何时有效、它解决什么问题。
- **必须：** `HREADY`、`HRESP`、`HSELx` 等信号不能互相替代；完成状态、选择关系和传输结果要分别说明。
- **必须：** 图中的 `[31:0]`、`[2:0]` 等位宽若只是示例，应注明不代表所有系统固定使用该宽度。
- **必须：** 描述确定的二进制电平时统一写成 `信号=0`、`信号=1`，例如 `HREADY=0`、`HWRITE=1`；不要写成 `=LOW` 或 `=HIGH`。只有在讨论抽象的高低电平概念而非具体赋值时，才使用“低电平”“高电平”等文字。

## 8. 重点样式

- **必须：** 正文保持默认颜色，重点少量着色即可。
- **必须：** 同一颜色在整篇文档中保持固定语义，不能随章节改变用途。
- **推荐：** 蓝色用于关键协议名或信号名，例如 `HREADY`、`HREADYOUT`。
- **推荐：** 橙色用于等待、限制和容易出错的条件。
- **推荐：** 绿色用于正确的数据方向和结论。
- **必须｜图后结论颜色：** “图片 → 图解 → 结论”结构中的结论统一使用绿色 `#63d297`，格式为 `> <span style="color:#63d297"><strong>结论：</strong>一句话结论。</span>`。
- **推荐：** 使用本地 Markdown 阅读器可显示的 HTML `<span style="color:...">` 标记颜色，不要给大段正文统一着色。
- **必须：** 颜色是辅助层次，不能替代标题、列表和清晰的句子。

## 9. 交叉链接和文件命名

- **必须：** 每篇章节精读和专题文档应链接回原始 PDF 或对应官方资料。
- **必须：** 章节精读中实际提出的问题集中放在本篇“问题记录与解答”章节，并为每个问题设置唯一、稳定的显式锚点。
- **必须：** 在最初引发问题的具体来源之后放置“相关问题”内部链接，准确跳转到对应问题。来源可能是图片图注、表格、规则段落或地址示例；链接不能仅放在同章节的概括位置代替实际提问位置。同一个问题确实由多个位置共同引出时，可以复用同一锚点和答案。
- **必须：** 问题锚点放在该问题的 `<details>` 之前，保证答案折叠时仍能跳转到可见的 `<summary>`；禁止把问题锚点藏在默认关闭的外层折叠块中。
- **推荐：** 只有需要被多篇文档共同引用的信号辨析或时序专题才单独建立 `.md`，并在相关正文附近链接；仅属于当前章节的问题不重复拆分文件。
- **推荐：** 专题文件名应能直接看懂主题，例如 `为什么Subordinate也接收HREADY.md`。
- **必须：** 图片和仓库内文档使用相对链接，确保在本地 Markdown 阅读器中可以打开。
- **必须：** 不重复创建同一主题的多个解释文件；已有文件应优先补充和复用。

## 10. 发布前检查

### 10.1 内容准确性

- [ ] 已逐段与官方原文核对角色、信号方向、等待条件和版本差异。
- [ ] 规范原意、理解提示和工程推断已经明确区分。
- [ ] 术语及信号名的写法、大小写和新旧称在全文一致。
- [ ] 公式、地址序列、编码和位宽示例已经至少手工复算一次。
- [ ] 每篇文档保留了与其类型相符的内容边界或适用范围。

### 10.2 图片与图表

- [ ] 官方图片文字清晰、图形完整，没有裁掉关键箭头，并保留规范要求的图注。
- [ ] 图片具有能说明图意的替代文本，所有图片相对链接均存在。
- [ ] 每张用于讲解协议的图片都按“图片 → 图解 → 结论”组织；时序图按时间顺序讲解，结构图按角色或数据路径讲解。
- [ ] 每个图后结论均直接回答该图对应的问题，并使用绿色 `#63d297` 的统一“结论：”格式。
- [ ] 问题截图完整保留提问上下文；尺寸与原图一致，条件允许时 SHA-256 也与原图一致。
- [ ] Mermaid 代码块已经闭合，节点和箭头表达单一、可验证的关系。
- [ ] 文档中没有残留 ASCII 文本框图。

### 10.3 结构与链接

- [ ] 标题层级连续且符合文档结构，没有从二级标题直接跳到四级标题。
- [ ] `<details>` 与 `</details>` 数量一致，嵌套关系正确，折叠后仍能识别章节标题。
- [ ] 每个实际问题具有唯一的显式锚点，“相关问题”链接能够到达正确位置。
- [ ] “问题记录与解答”的章节标题始终可见，每个问题独立折叠，问题锚点位于对应折叠块之前。
- [ ] 每个问题锚点至少有一个正文来源入口，且入口紧跟实际引发问题的图片、表格、规则或示例，没有被放到仅仅相近的概括位置。
- [ ] 所有仓库内 Markdown 相对链接均存在，没有重复创建已有专题。
- [ ] 确定电平全部采用 `=0`、`=1`，没有残留 `=LOW` 或 `=HIGH`。
- [ ] 学习目标已与自测题去重合并；实际提问单独集中在自测题之后，没有与自测题混放。
- [ ] 使用 `<details>` 的自测题中，所有参考答案均采用统一的 Markdown 引用块缩进，多段答案之间保留空行。
- [ ] 已在本地目标 Markdown 阅读器中预览颜色、表格、图片和 Mermaid 的实际效果。

## 11. 本规范的适用约定

后续新增或扩展 AHB 学习文档时，默认遵守本文件。若官方新版本与本规范中的示例冲突，以新版本官方文档为准，并在文档中记录差异。
